
```
@startuml
title Диаграмма последовательностей - Приёмка товара

skinparam sequenceArrowThickness 2
skinparam sequenceParticipantBackgroundColor #E3F2FD
skinparam sequenceLifeLineBackgroundColor #BBDEFB

actor "Кладовщик" as Worker
participant "WarehouseSystem" as WS
participant "Product" as P
participant "GoodsBatch" as GB

Worker -> WS: processReceipt(productId, quantity)
activate WS

WS -> WS: найти Product в products[] по id
WS -> P: <<создать/найти>> Product
P --> WS: product

WS -> GB: new GoodsBatch(product, quantity)
activate GB

GB -> GB: receive()
GB -> GB: изменить статус на "Принята"\nвывести лог
GB --> WS: готовая партия

deactivate GB

WS -> WS: добавить в batches[]
WS --> Worker: партия принята

deactivate WS
@enduml
```

```
@startuml
title Диаграмма последовательностей - Размещение товара на складе

skinparam sequenceArrowThickness 2
skinparam sequenceParticipantBackgroundColor #E3F2FD
skinparam sequenceLifeLineBackgroundColor #BBDEFB

actor "Кладовщик" as Worker
participant "WarehouseSystem" as WS
participant "GoodsBatch" as GB
participant "StorageCell" as SC

Worker -> WS: processPlacement(batchId)
activate WS

WS -> WS: найти партию в batches[]

WS -> SC: hasSpace()
activate SC
SC --> WS: true (есть место)
deactivate SC

WS -> SC: placeBatch(batch)
activate SC

SC -> SC: увеличить currentVolume
SC -> SC: связать партию с ячейкой
SC --> WS: размещено

deactivate SC

WS -> GB: cell = storageCell
activate GB
GB --> WS: ячейка закреплена
deactivate GB

WS --> Worker: товар размещён

deactivate WS
@enduml
```

```
@startuml
title Диаграмма последовательностей - Отбор и комплектация заказа

skinparam sequenceArrowThickness 2
skinparam sequenceParticipantBackgroundColor #E3F2FD
skinparam sequenceLifeLineBackgroundColor #BBDEFB

actor "Кладовщик" as Worker
participant "WarehouseSystem" as WS
participant "WarehouseOrder" as WO
participant "GoodsBatch" as GB
participant "StorageCell" as SC

Worker -> WS: processPicking(orderId)
activate WS

WS -> WS: найти заказ в orders[]
WS -> WO: pickItem()
activate WO

WO -> GB: получить batch.cell.address
activate GB
GB --> WO: адрес ячейки
deactivate GB

WO -> WO: построить маршрут обхода\nпо адресам ячеек

WO -> GB: уменьшить quantity
activate GB
GB --> WO: количество списано
deactivate GB

WO -> SC: освободить currentVolume
activate SC
SC --> WO: объем освобождён
deactivate SC

WO --> WS: заказ укомплектован
deactivate WO

WS --> Worker: комплектация завершена
deactivate WS

@enduml
```

```
@startuml
title Диаграмма последовательностей - Инвентаризация

skinparam sequenceArrowThickness 2
skinparam sequenceParticipantBackgroundColor #E3F2FD
skinparam sequenceLifeLineBackgroundColor #BBDEFB

actor "Оператор" as Operator
participant "WarehouseSystem" as WS
participant "StocktakeSession" as SS
participant "StorageCell" as SC
participant "GoodsBatch" as GB

Operator -> WS: processStocktake(sessionId)
activate WS

WS -> WS: найти сессию в stocktakes[]
WS -> SS: создать/получить сессию
activate SS

loop для каждой ячейки cells[]

    WS -> SC: получить партию
    activate SC
    SC --> WS: goodsBatch
    deactivate SC

    WS -> GB: получить quantity
    activate GB
    GB --> WS: системное количество
    deactivate GB

    WS -> SS: recordFact(cell, batch, actualQty)
    SS -> SS: сравнить факт с batch.quantity

    alt Совпадение
        SS --> WS: "OK - совпадение"
    else Недостача
        SS --> WS: "Недостача"
    else Излишек
        SS --> WS: "Излишек"
    end

end

SS --> WS: инвентаризация завершена
deactivate SS

WS --> Operator: результаты сохранены
deactivate WS

@enduml
```

```
@startuml
title Диаграмма последовательностей - Отгрузка товара

skinparam sequenceArrowThickness 2
skinparam sequenceParticipantBackgroundColor #E3F2FD
skinparam sequenceLifeLineBackgroundColor #BBDEFB

actor "Менеджер" as Manager
participant "WarehouseSystem" as WS
participant "Shipment" as SH
participant "WarehouseOrder" as WO

Manager -> WS: processShipment(shipmentId)
activate WS

WS -> WS: найти отгрузку в shipments[]
WS -> SH: dispatch()
activate SH

loop для каждого заказа orders[]

    SH -> WO: изменить статус на "Отгружен"
    activate WO

    WO --> SH: статус обновлён

    deactivate WO

end

SH -> SH: сгенерировать итоговый лог
SH --> WS: отгрузка оформлена

deactivate SH

WS --> Manager: отгрузка завершена

deactivate WS

@enduml
```
