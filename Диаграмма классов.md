
```
@startuml
left to right direction
skinparam classAttributeIconSize 0

class WarehouseSystem {
  + products: Product[]
  + cells: StorageCell[]
  + batches: GoodsBatch[]
  + orders: WarehouseOrder[]
  + stocktakes: StocktakeSession[]
  + shipments: Shipment[]
  + processReceipt()
  + processPlacement()
  + processPicking()
  + processStocktake()
  + processShipment()
  + createOrder()
}

class Product {
  + id: int
  + name: string
  + sku: string
}

class StorageCell {
  + id: int
  + address: string
  + maxCapacity: int
  + currentVolume: int
  + hasSpace(): boolean
  + placeBatch()
}

class GoodsBatch {
  + id: int
  + product: Product
  + quantity: int
  + cell: StorageCell
  + receive()
}

class WarehouseOrder {
  + id: int
  + items: GoodsBatch[]
  + pickItem()
}

class StocktakeSession {
  + id: int
  + recordFact()
}

class Shipment {
  + id: int
  + orders: WarehouseOrder[]
  + dispatch()
}

' Связи центрального мастера
WarehouseSystem "1" o-- "*" Product : Хранит каталог
WarehouseSystem "1" o-- "*" StorageCell : Хранит ячейки
WarehouseSystem "1" o-- "*" GoodsBatch : Хранит остатки
WarehouseSystem "1" o-- "*" WarehouseOrder : Хранит заказы
WarehouseSystem "1" o-- "*" StocktakeSession : Хранит сессии
WarehouseSystem "1" o-- "*" Shipment : Хранит отгрузки

' Доменные связи
GoodsBatch "*" --> "1" Product : Ссылается на
GoodsBatch "*" --> "0..1" StorageCell : Закреплен за
StorageCell "1" o-- "*" GoodsBatch : Хранит в себе
WarehouseOrder "*" o-- "*" GoodsBatch : Содержит
Shipment "1" o-- "*" WarehouseOrder : Объединяет
StocktakeSession "*" --> "*" StorageCell : Проверяет

@enduml
```
